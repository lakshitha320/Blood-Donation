server:
  port: 8083

spring:
  application:
    name: inventory-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/inventory_db}

api:
  security:
    key: ${API_KEY:blood_donation_secret_key_2026}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
