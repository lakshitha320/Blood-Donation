server:
  port: 8082

spring:
  application:
    name: inventory-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/inventory_service

api:
  key: ${API_SECURITY_KEY:blood_donation_secret_key_2026}
  security:
    key: ${API_SECURITY_KEY:blood_donation_secret_key_2026}
