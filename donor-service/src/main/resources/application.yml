server:
  port: 8082

spring:
  application:
    name: donor-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/donor_db}

api:
  security:
    key: ${API_KEY:my-secret-api-key-12345}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
